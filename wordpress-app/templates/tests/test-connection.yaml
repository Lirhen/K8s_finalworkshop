apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-app.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
